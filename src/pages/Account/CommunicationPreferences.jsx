export default function CommunicationPreferences({ value, loading, saving, onChange, onSave }) {
  const option = (name, title, detail) => <label><input type="checkbox" checked={Boolean(value[name])} onChange={onChange(name)} /><span><strong>{title}</strong><small>{detail}</small></span></label>;
  return <section className="communication-preferences"><header><span>Communication preferences</span><h2>Choose how we keep you informed</h2><p>Changes apply to future operational updates. Password resets, email verification and important security messages are always sent.</p></header>{loading ? <p className="communication-loading">Loading communication preferences…</p> : <form onSubmit={onSave}><div className="communication-grid"><fieldset><legend>Email</legend>{option("emailOrderUpdates", "Orders and cancellations", "Cancellation outcomes and related refund information.")}{option("emailShipmentUpdates", "Shipment and delivery", "Courier, tracking and delivery-status changes.")}{option("emailReturnRefundUpdates", "Returns and refunds", "Return decisions and completed refund records.")}</fieldset><fieldset><legend>Notification centre</legend>{option("inAppOrderUpdates", "Orders and cancellations", "Order confirmations and cancellation outcomes.")}{option("inAppShipmentUpdates", "Shipment and delivery", "Progress updates shown in your notification centre.")}{option("inAppReturnRefundUpdates", "Returns and refunds", "Return and refund status changes.")}{option("inAppReviewUpdates", "Review moderation", "Whether a submitted review was approved or rejected.")}</fieldset></div><button disabled={saving}>{saving ? "Saving…" : "Save communication preferences"}</button></form>}</section>;
}
